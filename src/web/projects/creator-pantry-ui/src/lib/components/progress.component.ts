import { ChangeDetectionStrategy, Component, computed, input } from '@angular/core';
@Component({selector:'cp-progress',standalone:true,template:`<div class="meta"><span>{{label()}}</span><strong>{{value()}}%</strong></div><div class="track" role="progressbar" [attr.aria-label]="label()" [attr.aria-valuenow]="normalized()" aria-valuemin="0" aria-valuemax="100"><span [style.width.%]="normalized()"></span></div>`,styles:[`:host{display:grid;gap:.4rem}.meta{display:flex;justify-content:space-between;color:var(--cp-text-muted);font-size:var(--cp-font-size-xs)}.track{height:.4rem;border-radius:var(--cp-radius-full);overflow:hidden;background:var(--cp-surface-subtle)}.track span{display:block;height:100%;border-radius:inherit;background:var(--cp-primary);transition:width var(--cp-duration-normal) var(--cp-ease)}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class CpProgressComponent{readonly value=input(0);readonly label=input('Progress');readonly normalized=computed(()=>Math.min(100,Math.max(0,this.value())));}
